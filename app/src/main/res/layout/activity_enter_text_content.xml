<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/rounded_corners"
    android:padding="16dp"
    tools:context=".ui.EnterTextContentActivity">

    <EditText
        android:id="@+id/enter_text_content_edit_text"
        android:layout_width="350dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/enter_text_content_text_view"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="20dp"
        android:ems="10"
        android:hint="Enter some content..."
        android:inputType="text" />

    <TextView
        android:id="@+id/enter_text_content_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="20dp"
        android:text="Enter some content"
        android:textColor="#FF424242" />

    <Button
        android:id="@+id/enter_text_result_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/enter_text_content_edit_text"
        android:layout_centerHorizontal="true"
        android:background="@drawable/button_design"
        android:text="OK"
        android:textColor="#FF424242" />
</RelativeLayout>
